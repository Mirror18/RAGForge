# Long Retrieval Design

这是一篇合成的长文，用于验证 long-document chunk boundaries。

## Section 1: normalized path
第 1 节记录 normalized path 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 1：当输入版本固定为 phase0-benchmark-1.0.0 时，normalized path 应保留原始 document_id 和 space_id。

## Section 2: space filter
第 2 节记录 space filter 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 2：当输入版本固定为 phase0-benchmark-1.0.0 时，space filter 应保留原始 document_id 和 space_id。

## Section 3: deterministic retry
第 3 节记录 deterministic retry 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 3：当输入版本固定为 phase0-benchmark-1.0.0 时，deterministic retry 应保留原始 document_id 和 space_id。

## Section 4: normalized path
第 4 节记录 normalized path 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 4：当输入版本固定为 phase0-benchmark-1.0.0 时，normalized path 应保留原始 document_id 和 space_id。

## Section 5: parser boundary
第 5 节记录 parser boundary 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 5：当输入版本固定为 phase0-benchmark-1.0.0 时，parser boundary 应保留原始 document_id 和 space_id。

## Section 6: normalized path
第 6 节记录 normalized path 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 6：当输入版本固定为 phase0-benchmark-1.0.0 时，normalized path 应保留原始 document_id 和 space_id。

## Section 7: parser boundary
第 7 节记录 parser boundary 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 7：当输入版本固定为 phase0-benchmark-1.0.0 时，parser boundary 应保留原始 document_id 和 space_id。

## Section 8: normalized path
第 8 节记录 normalized path 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 8：当输入版本固定为 phase0-benchmark-1.0.0 时，normalized path 应保留原始 document_id 和 space_id。

## Section 9: chunk provenance
第 9 节记录 chunk provenance 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 9：当输入版本固定为 phase0-benchmark-1.0.0 时，chunk provenance 应保留原始 document_id 和 space_id。

## Section 10: deterministic retry
第 10 节记录 deterministic retry 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 10：当输入版本固定为 phase0-benchmark-1.0.0 时，deterministic retry 应保留原始 document_id 和 space_id。

## Section 11: space filter
第 11 节记录 space filter 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 11：当输入版本固定为 phase0-benchmark-1.0.0 时，space filter 应保留原始 document_id 和 space_id。

## Section 12: parser boundary
第 12 节记录 parser boundary 的可复现观察。该段只描述公开的教学场景，不包含个人内容、真实客户信息或生产凭据。
测试结论 12：当输入版本固定为 phase0-benchmark-1.0.0 时，parser boundary 应保留原始 document_id 和 space_id。
