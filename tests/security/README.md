# Security Tests

覆盖 space RBAC、Session/CSRF、service token、未授权 cloud egress、SSRF/DNS rebinding/redirect、恶意上传、prompt injection、citation forgery、secret redaction 和依赖/镜像扫描。

恶意样本必须合法、安全地保存；高风险 payload 放隔离测试资产并避免被普通工具误执行。
