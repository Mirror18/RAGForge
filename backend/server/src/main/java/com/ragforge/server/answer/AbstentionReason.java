package com.ragforge.server.answer;

public enum AbstentionReason {
    NO_EVIDENCE,
    LOW_CONFIDENCE,
    EVIDENCE_CONFLICT,
    POLICY_BLOCKED,
    SPACE_ACCESS_DENIED,
    PROVIDER_UNAVAILABLE,
    CANCELLED
}
