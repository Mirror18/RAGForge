spring:
  datasource:
    hikari:
      maximum-pool-size: 4
  flyway:
    clean-disabled: false
management:
  endpoints:
    web:
      exposure:
        include: health
app:
  session:
    cookie-secure: false
    cookie-name: RAGFORGE_SESSION
