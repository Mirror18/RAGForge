# Model Profile Samples

计划样例：Ollama chat、local embedding、local rerank、generic OpenAI-compatible chat。每个配置声明 capabilities、context/dimension、timeouts、rate/concurrency、parameters 和 pricing metadata；连接/密钥单独管理。

示例只有通过 Provider contract test 后才能作为 seed 导入。

